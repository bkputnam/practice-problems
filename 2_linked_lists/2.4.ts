import { getTail, LinkedList } from "./linked_list.js";

function partition(head: LinkedList, value: number) {
    const leftPartition = new LinkedList();
    const rightPartition = new LinkedList();
    while (current !== tail) {

    }
}